spring.application.name=lets-play

# Server (matches OpenAPI spec)
server.port=8080

# MongoDB: default local. Override with MONGODB_URI (e.g. in .env or cloud env)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/letsplay}
